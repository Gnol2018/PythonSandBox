from typing import List

class Solution:
    def maxArea(self, height: List[int]) -> int:
        # find two biggest numbers in array
        # loop through to have the first number as pointer
        # second pointer is the next one
        biggest numb
        for i in len(height):

        return result

height = [1,8,6,2,5,4,8,3,7]
result = Solution()
print(result.maxArea(height))